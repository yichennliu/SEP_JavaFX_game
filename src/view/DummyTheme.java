package view;

import javafx.scene.image.Image;
import model.enums.Token;
import model.themeEditor.HashMap2D;
import model.themeEditor.SpriteSheet;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;


public class DummyTheme {

private Theme theme;


    public static Theme getTheme(){
//        HashMap2D<Token,Theme.FeldType,List<SpriteSheet>> images = new HashMap2D();
//        images.put(Token.MUD,Theme.FeldType.FOUREDGE, Arrays.asList(new SpriteSheet(new Image("Hier die URL des Bildes"),20)));
//        images.put(Token.PATH, Theme.FeldType.FOUREDGE, Arrays.asList(new SpriteSheet(new Image(""),20)));
//        //...
//        return new Theme(images,"Test-Theme");
        return null;
    }
}
